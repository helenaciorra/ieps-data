import React from 'react';
import Productions from '../../containers/Productions';

const ProductionsPage = ({ location }) => {
  return <Productions location={location} />;
};

export default ProductionsPage;
