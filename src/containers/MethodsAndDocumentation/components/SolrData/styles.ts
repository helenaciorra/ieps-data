import styled from 'styled-components';
import { azure } from '../../../../constants/theme';

export const PageTitle = styled.h1`
  color: ${azure};
  margin-bottom: 3rem;
  font-size: 2.4rem;
  line-height: 2.8rem;
  display: block;
`;
